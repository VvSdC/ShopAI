[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  REACT_APP_API_URL = "https://YOUR-BACKEND.onrender.com/shopai"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
