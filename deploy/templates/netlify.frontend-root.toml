[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  # Set REACT_APP_API_URL in Netlify UI — do not hardcode placeholder URLs here.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
